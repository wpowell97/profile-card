services:
  - type: web
    name: profile-card-app
    runtime: dotnet
    repo: https://github.com/wpowell97/profile-card
    branch: main
    buildCommand: dotnet build
    startCommand: dotnet ProfileCardApp.dll
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
